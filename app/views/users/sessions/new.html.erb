<div class="auth-container auth-container-light">
  <div class="auth-card auth-card-light">
    <div class="auth-header">
      <h1 class="auth-title">User Management</h1>
      <p class="auth-subtitle">Welcome!</p>
      <p class="auth-description">Enter your credentials to access the system</p>
    </div>

    <%= form_with model: resource, as: resource_name, url: session_path(resource_name), local: true, data: { turbo: false }, class: 'auth-form' do |form| %>
      <div class="auth-input-group">
        <i class="bi bi-envelope auth-icon"></i>
        <%= form.email_field :email, autofocus: true, autocomplete: 'email', class: 'auth-input', placeholder: 'Enter your email', required: true %>
      </div>

      <div class="auth-input-group">
        <i class="bi bi-lock auth-icon"></i>
        <%= form.password_field :password, autocomplete: 'current-password', class: 'auth-input', placeholder: 'Enter your password', required: true, id: 'password-field' %>
        <i class="bi bi-eye auth-icon-right" id="toggle-password" style="cursor: pointer;"></i>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="auth-options">
          <div class="auth-checkbox">
            <%= form.check_box :remember_me, class: 'form-check-input', id: 'remember-me' %>
            <label for="remember-me" class="auth-checkbox-label">Remember me</label>
          </div>
          <%= link_to 'Forgot your password?', new_user_password_path, class: 'auth-forgot-link' %>
        </div>
      <% end %>

      <div class="auth-actions">
        <%= form.submit 'Sign In', class: 'auth-button auth-button-primary' %>
      </div>
    <% end %>

    <div class="auth-divider">
      <span>or</span>
    </div>

    <div class="auth-actions">
      <%= form_with url: guest_session_path, method: :post, local: true, class: 'd-inline w-100' do |form| %>
        <button type="submit" class="auth-button auth-button-secondary">
          <i class="bi bi-person-badge me-2"></i>
          Guest Access
        </button>
      <% end %>
    </div>

    <div class="auth-footer">
      <p class="auth-footer-text">
        New here? 
        <%= link_to 'Create an account and try it for free', new_user_registration_path, class: 'auth-link' %>
      </p>
      <p class="auth-copyright">© 2025 Fullstack Developer Test. All rights reserved.</p>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const togglePassword = document.getElementById('toggle-password');
    const passwordField = document.getElementById('password-field');
    
    if (togglePassword && passwordField) {
      togglePassword.addEventListener('click', function() {
        const type = passwordField.getAttribute('type') === 'password' ? 'text' : 'password';
        passwordField.setAttribute('type', type);
        togglePassword.classList.toggle('bi-eye');
        togglePassword.classList.toggle('bi-eye-slash');
      });
    }
  });
</script>

